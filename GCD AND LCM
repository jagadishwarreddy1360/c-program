#include<stdio.h>
int main()
{
    int n1=25,n2=125,lcm,gcd,i;
    for(i=1;i<=n1&&i<=n2;i++)
    {
        if(n1%i==0&&n2%i==0)
        {
            gcd=i;
        }
        lcm=(n1*n2)/gcd;
    }
    printf("the lcm of two numbers is %d and gcd of two numbers is %d",lcm,gcd);
    return 0;
}
